import React from 'react';
import AddDriver from '../../components/AddDriver/AddDriver';

const Drivers = () => {
  
  
  return (
    <div>
      <AddDriver/>
    </div>
  );
};

export default Drivers;